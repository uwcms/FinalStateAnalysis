
// Fake rate functions used to weight events in the VH analysis.
// This file auto-generated by make_fakerates.py
#include "TMath.h"
#include <iostream>

float fakerate_tau(float pt, float /*eta*/) {
    return 3.9114e+00*TMath::Landau(pt,2.6588e+01,2.2023e+00,0)+3.6032e-13;
}

float fakerate_tau_flat(float /*pt*/, float /*eta*/) {
    return 0.034837;
}

float fakerate_muQCD(float pt, float /*eta*/) {
    return 2.7847e+00*TMath::Landau(pt,1.5133e+01,2.7358e+00,0)+4.2364e-04;
}

float fakerate_muQCD_flat(float /*pt*/, float /*eta*/) {
    return 0.053157;
}

float fakerate_eMITQCD(float pt, float /*eta*/) {
    return 8.9667e-01*TMath::Landau(pt,1.6503e+01,2.0758e+00,0)+7.4855e-03;
}

float fakerate_eMITQCD_flat(float /*pt*/, float /*eta*/) {
    return 0.015741;
}

float fakerate_muHighPt(float pt, float /*eta*/) {
    return 4.0317e+00*TMath::Landau(pt,4.3490e-08,9.5040e+00,0)+1.7065e-10;
}

float fakerate_muHighPt_flat(float /*pt*/, float /*eta*/) {
    return 0.094655;
}

float fakerate_tauQCD(float pt, float /*eta*/) {
    return 2.8065e+00*TMath::Landau(pt,2.7653e+01,2.4198e+00,0)+3.2556e-11;
}

float fakerate_tauQCD_flat(float /*pt*/, float /*eta*/) {
    return 0.017839;
}

float fakerate_mu(float pt, float /*eta*/) {
    return 3.2687e+00*TMath::Landau(pt,1.4626e+01,4.1486e+00,0)+2.1439e-03;
}

float fakerate_mu_flat(float /*pt*/, float /*eta*/) {
    return 0.095181;
}

float fakerate_tauQCDTauPt(float pt, float /*eta*/) {
    return 8.4078e+00*TMath::Landau(pt,4.9816e-06,1.0886e+00,0)+5.4055e-03;
}

float fakerate_tauQCDTauPt_flat(float /*pt*/, float /*eta*/) {
    return 0.017839;
}

float fakerate_muHighPtQCDOnly(float pt, float /*eta*/) {
    return 3.9617e+00*TMath::Landau(pt,2.9077e+01,2.5939e+00,0)+8.6198e-03;
}

float fakerate_muHighPtQCDOnly_flat(float /*pt*/, float /*eta*/) {
    return 0.054960;
}

float fakerate_tauTauPt(float pt, float /*eta*/) {
    return 8.4868e-01*TMath::Landau(pt,6.8877e-08,6.2191e+00,0)+7.5787e-03;
}

float fakerate_tauTauPt_flat(float /*pt*/, float /*eta*/) {
    return 0.044513;
}

float fakerate_eMIT(float pt, float /*eta*/) {
    return 1.7189e+00*TMath::Landau(pt,1.2090e+01,1.9486e+00,0)+1.7139e-02;
}

float fakerate_eMIT_flat(float /*pt*/, float /*eta*/) {
    return 0.025028;
}


// ############################################################################
// Corresponding weight functions #############################################
// ############################################################################
float weight_tau(float pt, float eta) {
    float fakerate = fakerate_tau(pt, eta);
    float result = fakerate/(1. - fakerate);
    return result;
}

float weight_tau_flat(float pt, float eta) {
    float fakerate = fakerate_tau_flat(pt, eta);
    float result = fakerate/(1. - fakerate);
    return result;
}

float weight_muQCD(float pt, float eta) {
    float fakerate = fakerate_muQCD(pt, eta);
    float result = fakerate/(1. - fakerate);
    return result;
}

float weight_muQCD_flat(float pt, float eta) {
    float fakerate = fakerate_muQCD_flat(pt, eta);
    float result = fakerate/(1. - fakerate);
    return result;
}

float weight_eMITQCD(float pt, float eta) {
    float fakerate = fakerate_eMITQCD(pt, eta);
    float result = fakerate/(1. - fakerate);
    return result;
}

float weight_eMITQCD_flat(float pt, float eta) {
    float fakerate = fakerate_eMITQCD_flat(pt, eta);
    float result = fakerate/(1. - fakerate);
    return result;
}

float weight_muHighPt(float pt, float eta) {
    float fakerate = fakerate_muHighPt(pt, eta);
    float result = fakerate/(1. - fakerate);
    return result;
}

float weight_muHighPt_flat(float pt, float eta) {
    float fakerate = fakerate_muHighPt_flat(pt, eta);
    float result = fakerate/(1. - fakerate);
    return result;
}

float weight_tauQCD(float pt, float eta) {
    float fakerate = fakerate_tauQCD(pt, eta);
    float result = fakerate/(1. - fakerate);
    return result;
}

float weight_tauQCD_flat(float pt, float eta) {
    float fakerate = fakerate_tauQCD_flat(pt, eta);
    float result = fakerate/(1. - fakerate);
    return result;
}

float weight_mu(float pt, float eta) {
    float fakerate = fakerate_mu(pt, eta);
    float result = fakerate/(1. - fakerate);
    return result;
}

float weight_mu_flat(float pt, float eta) {
    float fakerate = fakerate_mu_flat(pt, eta);
    float result = fakerate/(1. - fakerate);
    return result;
}

float weight_tauQCDTauPt(float pt, float eta) {
    float fakerate = fakerate_tauQCDTauPt(pt, eta);
    float result = fakerate/(1. - fakerate);
    return result;
}

float weight_tauQCDTauPt_flat(float pt, float eta) {
    float fakerate = fakerate_tauQCDTauPt_flat(pt, eta);
    float result = fakerate/(1. - fakerate);
    return result;
}

float weight_muHighPtQCDOnly(float pt, float eta) {
    float fakerate = fakerate_muHighPtQCDOnly(pt, eta);
    float result = fakerate/(1. - fakerate);
    return result;
}

float weight_muHighPtQCDOnly_flat(float pt, float eta) {
    float fakerate = fakerate_muHighPtQCDOnly_flat(pt, eta);
    float result = fakerate/(1. - fakerate);
    return result;
}

float weight_tauTauPt(float pt, float eta) {
    float fakerate = fakerate_tauTauPt(pt, eta);
    float result = fakerate/(1. - fakerate);
    return result;
}

float weight_tauTauPt_flat(float pt, float eta) {
    float fakerate = fakerate_tauTauPt_flat(pt, eta);
    float result = fakerate/(1. - fakerate);
    return result;
}

float weight_eMIT(float pt, float eta) {
    float fakerate = fakerate_eMIT(pt, eta);
    float result = fakerate/(1. - fakerate);
    return result;
}

float weight_eMIT_flat(float pt, float eta) {
    float fakerate = fakerate_eMIT_flat(pt, eta);
    float result = fakerate/(1. - fakerate);
    return result;
}

